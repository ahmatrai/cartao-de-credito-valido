var function isValidCard() {
  var sentence = prompt("Insira o número do cartão");
  var element = document.getElementById("resultado");

  if (sentence !== "") {
  }

  element.innerHTML = "Não foi inserido um número válido"
  return false;
}
